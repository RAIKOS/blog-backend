let Router,postsCtrl;_654‍.x([["default",()=>_654‍.o]]);_654‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_654‍.w("./posts.ctrl",[["*",null,function(v){postsCtrl=v}]]);


const posts = new Router();

const printInfo = (ctx) => {
  ctx.body = {
    method: ctx.method,
    path: ctx.path,
    params: ctx.params,
  };
};

posts.get('/', postsCtrl.list);
posts.post('/', postsCtrl.write);

const post = new Router();
post.get('/', postsCtrl.read);
post.delete('/', postsCtrl.remove);
post.patch('/', postsCtrl.update);

posts.use('/:id', postsCtrl.checkObjectId, post.routes());

_654‍.d(posts);
